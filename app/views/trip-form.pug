extends index
block content 
    
  h1 Start a New Trip
  p.start-trip I'm going 
    input.text(id="activity" name="activity" type="text" default="on a run at the Volunteer Trail" required)
    and   and I should be back by 
    input(id="return-time" name="return-time" type="time" required).

    button.btn.btn-primary#start-trip Start Trip
  
  .trip-in-progress
    p#error
    h3.timer
    label(for="safe-code") When you get back, please enter your safe code.
    input.text(id="safe-code" name="safe-code" type="text" required)
    button.btn.btn-primary#safe-code-btn OK
  
    a(href='/trip/start')
      button.btn.btn-primary#start-over Start Another Trip
    

